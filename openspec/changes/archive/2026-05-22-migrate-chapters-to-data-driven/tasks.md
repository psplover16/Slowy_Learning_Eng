## 1. 型別與 helper（基礎，無依賴）

- [x] [P] 1.1 Create `src/modules/chapters/types.ts` realizing the "資料模組為 TS 而非 JSON" decision and the Chapter content data SHALL live in standalone TypeScript modules requirement: define and export the `ChapterData` interface with fields `headerTitleZh`, `headerTitleEn`, `headerPodcastLabel`, `headerLevelTag`, `headerTopicTag`, `mp3Src?: string | null`, `scenes: Scene[]`, `vocabGroups: VocabGroup[]`, `phrases: PhraseCard[]`, `breakdowns: SentenceBreakdown[]`; also export sub-interfaces `Scene` (with `id`, `no`, `titleZh`, `titleEn`, `sentences: SceneSentence[]`, `tags: WordTag[]`), `SceneSentence` (`en`, `tc`), `VocabGroup` (`title`, `items: VocabItem[]`), `VocabItem` (`id?`, `english`, `kk?`, `pos`, `meaning`, `example?`), `WordTag` (same shape as VocabItem), `PhraseCard` (`id`, `phrase`, `meaning`, `note`, `example`, `exampleTc`), `SentenceBreakdown` (`id?`, `sentence`, `translation`, `chunks: BreakdownChunk[]`), `BreakdownChunk` (`en`, `note`). Verification: `npm run typecheck` passes; module is importable from elsewhere.

- [x] [P] 1.2 Create `src/modules/chapters/utils/highlight.ts` realizing the "`hl()` helper 提取至 `src/modules/chapters/utils/highlight.ts`" decision and the Highlight helper SHALL be a shared utility module requirement: export a pure function `hl(text: string, targetId: string): string` that returns the exact string `<span data-target="${targetId}" class="underline decoration-terracotta underline-offset-2 cursor-pointer hover:text-terracotta transition-colors">${text}</span>`. Add a doc-comment noting the function does NOT escape `text` and is intended only for controlled chapter content (not user input). Also add a unit test `src/__tests__/highlight.test.ts` covering: (a) returned string contains `data-target="vocab-head-to"` for input `('head to', 'vocab-head-to')`; (b) text content is preserved verbatim. Verification: `npx vitest run src/__tests__/highlight.test.ts` passes.

## 2. 共用元件搬遷

- [x] [P] 2.1 Move the four chapter-presentation components from `src/modules/ch1/components/` to `src/modules/chapters/components/` realizing the "共用元件搬到 `src/modules/chapters/components/`" decision and the Shared chapter UI components SHALL live under src/modules/chapters/components/ requirement: copy `SceneBlock.vue`, `WordTag.vue`, `PhraseCard.vue`, `SentenceBreakdown.vue` into the new path with identical content (do not change templates, props, emits, or styles); the only allowed delta is internal import paths if any component imports siblings. Verification: each new file exists at the expected path; `git diff` shows file additions and no content modification; `npm run typecheck` after intermediate updates still parses these as valid components.

## 3. 抽取 Ch1 內容到 data 模組

- [x] 3.1 Create `src/modules/chapters/data/ch1.ts` realizing the "資料模組為 TS 而非 JSON" decision: import `hl` from `../utils/highlight` and types from `../types`; `export default` a `ChapterData` object containing the EXACT content currently inline in `src/modules/ch1/views/Ch1View.vue` — the 15-entry `scenes` array (including all hl-wrapped sentences), the 6-entry `vocabGroups` array, the 13-entry `phrases` array, the 30+ entry `breakdowns` array, plus `headerTitleZh='我的紐約之旅'`, `headerTitleEn='My Trip to New York City'`, `headerPodcastLabel='Slow English Podcast · B1'`, `headerLevelTag='程度 B1 初中級'`, `headerTopicTag='主題：旅行・童年回憶'`, `mp3Src=null`. Content MUST be byte-for-byte identical to current Ch1View — no text re-edits during migration. Verification: file exists; `import('./data/ch1')` resolves to an object satisfying `ChapterData`.

- [x] 3.2 Create `src/__tests__/chapterDataModule.test.ts` realizing the Chapter content data SHALL live in standalone TypeScript modules requirement: import the ch1 data module; assert (a) the default export has all required ChapterData fields; (b) `scenes.length === 15`, `vocabGroups.length === 6`, `phrases.length === 12`, `breakdowns.length >= 29`; (c) `scenes[0].id === 'scene-01'`; (d) the underlined-word anchor target ids from the previous Ch1View test (`vocab-layover`, `vocab-to-have-had`, `vocab-used-to`, `vocab-bring-back`) appear somewhere in the serialized data (use `JSON.stringify` then `includes` check); (e) `phrases.find(p => p.id === 'vocab-bring-back')` exists. Verification: `npx vitest run src/__tests__/chapterDataModule.test.ts` shows all green after task 3.1.

## 4. Generic ChapterView 元件

- [x] 4.1 Create `src/modules/chapters/ChapterView.vue` realizing the "ChapterView 透過 `useRoute().params.id` 解析" decision (note: this design names the decision after `useRoute` but the final implementation receives chapter id via the `id` prop injected by `props: (route) => ({ id: route.name })` in router/index.ts; this keeps the component testable without a full router) and the A generic ChapterView SHALL render any chapter from its data module requirement: accept prop `id: string`; on mount, look up the chapters config entry by id (import `chapters` from `'../../shared/config/chapters'`), invoke `entry.dataLoader()`, await the resolved data, store it in a reactive ref `chapterData`; render the layout identical to the current Ch1View (Mp3Player with `chapterData.mp3Src`, header bound to header fields, `<SectionQuickNav>` with sections generated from `id` (e.g., `${id}-section-bilingual`/etc. with labels '全文'/'單字'/'片語'/'句型'), 4 `<section>` elements with ids `${id}-section-*` containing scene/vocab/phrase/breakdown content, `<BackToWordFab>` and `<BackToTopFab>` wired exactly as in current Ch1View); show a single line "載入中…" placeholder while `chapterData` is null; show a "找不到此章節" message if the chapters config lookup returns undefined. Verification: typecheck passes; the component file exists and exports a Vue SFC.

- [x] 4.2 Rewrite `src/__tests__/Ch1View.test.ts` to test ChapterView mounted with `props.id='ch1'` realizing the "Ch1View.test.ts → ChapterView.test.ts，所有 ch1 行為仍保留為「ch1 instance」 case" decision and the All previously verified Ch1 behaviors SHALL remain intact requirement: rename the file to `src/__tests__/ChapterView.test.ts`; change the import from `Ch1View` to `ChapterView`; change `mount(Ch1View, ...)` to `mount(ChapterView, { props: { id: 'ch1' }, global: ... })`; if data is loaded asynchronously, add `await flushPromises()` after mount before assertions; preserve every existing assertion verbatim — scene count (15), scene-01 title contains '凌晨出發', `#vocab-layover` exists, `#vocab-to-have-had` exists, `#vocab-used-to` exists, `#vocab-bring-back` exists, WordTag count >= 60, underlined data-target count > 0, sentence breakdown count >= 29, all four section ids `ch1-section-*` exist, ch1-section-bilingual contains 15 scene blocks, quick-nav has 4 buttons '全文/單字/片語/句型', `[data-testid="quick-nav-ch1-section-*"]` exists for each, BackToTopFab integration present. Keep IntersectionObserver mock from current test. Verification: `npx vitest run src/__tests__/ChapterView.test.ts` shows all assertions green after task 4.1 + 3.1.

## 5. 設定與路由

- [x] 5.1 Extend `src/shared/config/chapters.ts` realizing the "chapters.ts schema 擴充：加 `dataLoader`" decision and the chapters config SHALL declare a per-chapter dataLoader requirement: import `type { ChapterData } from '../../modules/chapters/types'`; extend the `ChapterEntry` interface with `dataLoader: () => Promise<{ default: ChapterData }>`; add `dataLoader: () => import('../../modules/chapters/data/ch1')` to the ch1 entry. Verification: typecheck passes; calling `chapters[0].dataLoader()` resolves to an object with `default` matching `ChapterData`.

- [x] 5.2 Refactor `src/app/router/index.ts` realizing the "路由註冊改為從 chapters config 自動生成" decision and the Vue Router SHALL register chapter routes from the chapters config requirement: import `chapters` from `'../../shared/config/chapters'`; build a `chapterRoutes` array by mapping each entry to `{ path: chapter.path, name: chapter.id, component: () => import('../../modules/chapters/ChapterView.vue'), props: (route) => ({ id: route.name as string }) }`; in the `createRouter({ routes: [...] })` call, replace the existing literal `/ch1` route with `...chapterRoutes`; keep the `/` and `/grammar` routes literal (they are not chapter routes). The final router file MUST NOT contain the literal strings `'/ch1'` or `'ch1'` anywhere. Verification: typecheck passes; `router.getRoutes()` includes one route with `path === '/ch1'` and `name === 'ch1'`.

- [x] 5.3 Update `src/__tests__/router.test.ts` to assert chapter routes come from the config realizing the Vue Router SHALL register chapter routes from the chapters config requirement: add assertions (a) `router.resolve('/ch1').matched[0].name === 'ch1'`; (b) the resolved route's component is the generic ChapterView (assert by name or by checking `component.__file` if available, otherwise dynamically import the route's component lazy function and check its module path); (c) chapters config length matches the number of registered chapter routes (filter by name being in the chapters config id set). Verification: `npx vitest run src/__tests__/router.test.ts` shows all assertions green.

## 6. 清理舊路徑

- [x] 6.1 Delete the entire `src/modules/ch1/` directory realizing the Shared chapter UI components SHALL live under src/modules/chapters/components/ requirement (which explicitly mandates removal of the `src/modules/ch1` directory): remove `src/modules/ch1/views/Ch1View.vue`, `src/modules/ch1/components/SceneBlock.vue`, `src/modules/ch1/components/WordTag.vue`, `src/modules/ch1/components/PhraseCard.vue`, `src/modules/ch1/components/SentenceBreakdown.vue`, and the parent `src/modules/ch1/` folder itself; grep the repo for any remaining `'../modules/ch1/'` or `'modules/ch1/'` import strings and update them to point at `src/modules/chapters/`; remove the old `src/__tests__/Ch1View.test.ts` file (it was renamed in task 4.2). Verification: `Test-Path src/modules/ch1` returns `False`; `npm run typecheck` and `npm run test:unit` pass — confirming no dangling imports reference the deleted directory.

## 7. 驗證與回歸

- [x] 7.1 Run the full local validation chain `npm run typecheck && npm run test:unit && npm run build` and confirm all three pass with zero errors: typecheck reports no type errors, vitest shows all test files green (including new `chapterDataModule.test.ts`, `highlight.test.ts`, the migrated `ChapterView.test.ts`, the updated `router.test.ts`, and every previously-green test file), build completes successfully and emits a separate chunk in `dist/assets/` for the ch1 data module (verifying Vite code-split via dynamic import works as designed). Verification: all three commands exit 0; manually inspect `dist/assets/` listing for a chunk filename containing `ch1` (or equivalent hash-with-source-map verifying the dynamic import was split).

- [x] 7.2 Manual viewport verification: run `npm run dev`, open `/ch1` in Chrome DevTools with iPhone 14 preset (390×844), and confirm by inspection that the page renders IDENTICALLY to the pre-migration state: header bilingual title visible, MP3 player hidden (since `mp3Src=null`), quick-nav toolbar with 4 buttons below header, 4 sections rendered with correct content (15 scenes / 6 vocab groups / 13 phrases / 30+ breakdowns), clicking quick-nav buttons smooth-scrolls with section heading visible below NavBar, clicking an underlined word triggers BackToWordFab, scrolling past quick-nav shows BackToTopFab, both fabs stack correctly when both visible, NavBar 內容 dropdown selecting "我的紐約之旅" lands on `/ch1`. Verification: all behaviors visually confirmed; no console errors during navigation or scroll.
