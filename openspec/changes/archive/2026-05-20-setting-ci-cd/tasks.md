## 1. 測試契約先行

- [x] [P] 1.1 建立 `Package script contract` 與 `Build-time base path normalization function` 的單元測試契約於 `src/__tests__/packageScripts.test.ts` 與 `src/__tests__/viteBasePath.test.ts`：前者檢查 `lint`、`typecheck`、`test:unit`、`build`、`test:e2e`、`test:ci` 六個 script 存在且 `test:ci` 依序串接前 5 個；後者匯入 `vite.config.ts` 匯出的 `normalizeBasePath` 函式，逐一驗證 spec input/output 表所有 cases（undefined/null/空字串/空白/`/`、缺前斜線、缺尾斜線、production、staging 與多段子路徑）。完成定義：`npm run test:unit -- packageScripts viteBasePath` 在實作前可偵測缺口、實作後通過。
- [x] [P] 1.2 建立 `CI workflow validation`、`CI diagnostics artifact upload` 與 `CD workflow deployment targets` 的 workflow 契約測試於 `src/__tests__/githubActionsWorkflows.test.ts`：以 YAML parser 讀 `.github/workflows/ci.yml` 與 `cd.yml`，斷言 workflow `name`、`on` triggers、`runs-on`、Node.js 22、`cache: npm`、step 順序（checkout → setup-node → npm ci → lint → typecheck → test:unit → build → install chromium → test:e2e）、`if: failure()` 上傳 `playwright-diagnostics`、`if-no-files-found: ignore`、CD `permissions.contents: write`、`concurrency.cancel-in-progress: true`、`dev → staging` 與 `main → production` 的 env mapping。完成定義：`npm run test:unit -- githubActionsWorkflows` 在實作前紅、實作後綠。
- [x] [P] 1.3 建立 `Protected GitHub Pages publishing` 的 publish script 契約測試於 `src/__tests__/publishPages.test.ts`：匯入 `syncPublishedSite`、`formatPublishSummary`、`formatNoPublishChangesMessage`，覆蓋 production 保留 `.git/.nojekyll/CNAME/staging`、staging 不影響 production root、staging 刪除含 `/src/` 的 stale `index.html`、空 dist throw、未知 target throw、複製時跳過 `.gz`/`.br`、結束時寫 `.nojekyll`。完成定義：`npm run test:unit -- publishPages` 實作後通過。
- [x] [P] 1.4 建立 `CI/CD documentation` 與 `First-deploy operational prerequisites` 的文件契約測試於 `src/__tests__/readmeCiCdDocs.test.ts`：讀 README.md 與 PROJECT_ARCHITECTURE.md，斷言 README 含本地驗證指令對照（6 個 npm script）、CI 觸發說明、CD 觸發說明、`dev → staging` 與 `main → production` 映射、production URL 範式（`https://*.github.io/Slowy_Learning_Eng/`）、staging URL 範式、Pages source 設定步驟、branch protection 建議、以及一個含「first-deploy」或「首次部署」字樣的章節列出 6 個操作步驟；PROJECT_ARCHITECTURE 含新增檔案清單與 base path → target → URL 對照。完成定義：`npm run test:unit -- readmeCiCdDocs` 實作後通過。
- [x] [P] 1.5 建立 `E2E testing infrastructure` 的 Playwright app shell smoke 測試於 `tests/e2e/app-shell.smoke.spec.ts`：deep-link 進入 `/`、`/grammar`、`/ch1`，每條路由斷言主要內容（NavBar、route content container）渲染且整個測試期間累計 `console.error` 次數為 0。完成定義：在 `playwright.config.ts` 與相關 script 完成後 `npm run test:e2e` 通過。

## 2. 驗證工具鏈與 package scripts

- [x] 2.1 依「Add ESLint and Playwright as validation-only dependencies」決策實作 `ESLint configuration baseline`：在 `package.json` 加入 ESLint、`@eslint/js`、`@typescript-eslint/eslint-plugin`、`@typescript-eslint/parser`、`vue-eslint-parser`、`eslint-plugin-vue`、`globals`、`@playwright/test` 為 devDependencies；建立 `eslint.config.js` flat config，組合 `js.configs.recommended` + `tsPlugin.configs.recommended`（套 `.ts`/`.tsx`，關掉 `no-undef`）+ `vuePlugin.configs['flat/recommended']`（套 `.vue`，關掉 `vue/multi-word-component-names`、`vue/html-self-closing`、`vue/max-attributes-per-line`、`vue/singleline-html-element-content-newline`、`vue/attributes-order`）；ignore `dist/**`、`build/**`、`coverage/**`、`node_modules/**`；對 `scripts/**/*.mjs` 套 Node globals。完成定義：`npm run lint` 不報設定錯誤，且若有既有 lint error 在同一變更內修正。
- [x] 2.2 依「Keep package scripts as the single CI contract」實作 `Package script contract`：`package.json` 提供 `lint`（`eslint . --ext .ts,.vue`）、`typecheck`（`vue-tsc --noEmit -p tsconfig.app.json`，若 `tsconfig.app.json` 不存在則建立）、`test:unit`（`vitest run`）、`build`（typecheck && `vite build`）、`test:e2e`（`playwright test`）、`test:ci`（前 5 個依序 `&&` 串接）；新增 `playwright.config.ts` skeleton 以避免 `test:e2e` 在 task 3 之前 import error。完成定義：`npm run test:unit -- packageScripts` 通過。

## 3. Playwright config 與 e2e server 切換

- [x] 3.1 依「Playwright config switches between dev server and preview server by CI flag」決策實作 `E2E testing infrastructure`：完成 `playwright.config.ts`——`testDir: './tests/e2e'`、`baseURL: http://127.0.0.1:${PLAYWRIGHT_PORT ?? 4173}`、`webServer.command: process.env.CI ? 'npm run preview ...' : 'npm run dev ...'`、`reuseExistingServer: !process.env.CI`、`retries: process.env.CI ? 2 : 0`、只啟 Chromium project、HTML reporter `open: 'never'`、timeout 120000。完成定義：本機 `npm run test:e2e` 啟動 dev server 並通過 task 1.5 的 smoke；設 `CI=1 npm run test:e2e` 啟動 preview server 也通過。

## 4. Base path 對齊

- [x] 4.1 依「Use a single normalized base path for every deployment-aware consumer」實作 `Build-time base path normalization function`：在 `vite.config.ts` 加入並 named export `normalizeBasePath(value: string | undefined | null): string`，依 spec 表所列規則處理；在 `defineConfig` 中以 `loadEnv` 讀 `VITE_APP_BASE_PATH`、呼叫一次、把結果套到 Vite `base`、PWA manifest `start_url` 與 icon `src`、Workbox `navigateFallback`。完成定義：`npm run test:unit -- viteBasePath` 通過，且 `npm run build` 無錯誤輸出。
- [x] 4.2 依「Use a single normalized base path for every deployment-aware consumer」實作 `GitHub Pages base path alignment`：將 `src/app/router/index.ts` 中 Vue Router 改用 `createWebHistory(import.meta.env.BASE_URL)`；確認 `vite.config.ts` 的 PWA manifest 與 SW 設定全部從同一個 `appBasePath` 變數讀。完成定義：本機以 `VITE_APP_BASE_PATH=/Slowy_Learning_Eng/staging/ npm run build` 後 `dist/index.html` 與 `manifest.webmanifest` 內所有路徑前綴為 `/Slowy_Learning_Eng/staging/`；以 `VITE_APP_BASE_PATH=/Slowy_Learning_Eng/ npm run build` 結果為 `/Slowy_Learning_Eng/`。

## 5. Workflow 與發布腳本

- [x] 5.1 依「Split CI and CD workflows by responsibility」實作 `CI workflow validation` 與 `CI diagnostics artifact upload`：建立 `.github/workflows/ci.yml`——`name: CI`、`on.pull_request` 與 `on.push.branches-ignore: [gh-pages]`、`permissions.contents: read`、`runs-on: ubuntu-latest`、`actions/setup-node@v4` Node 22 + `cache: npm`、步驟 checkout → setup-node → `npm ci` → lint → typecheck → test:unit → build → `npx playwright install chromium` → test:e2e；最後一步 `if: failure()` `actions/upload-artifact@v4` 上傳 `playwright-report/`、`test-results/` 為 `playwright-diagnostics`，`if-no-files-found: ignore`。完成定義：`npm run test:unit -- githubActionsWorkflows` 通過 CI workflow 相關斷言。
- [x] 5.2 依「Split CI and CD workflows by responsibility」實作 `CD workflow deployment targets`：建立 `.github/workflows/cd.yml`——`name: CD`、`on.push.branches: [dev, main]`、`permissions.contents: write`、`concurrency.group: ${{ github.workflow }}-${{ github.ref }}` 與 `cancel-in-progress: true`；步驟：checkout（`fetch-depth: 0`）→ setup-node Node 22 → `npm ci` → bash 分支判斷把 `PUBLISH_TARGET` 與 `VITE_APP_BASE_PATH`/`VITE_APP_START_URL`（main → production+`/Slowy_Learning_Eng/`；dev → staging+`/Slowy_Learning_Eng/staging/`）寫入 `$GITHUB_ENV` → `npm run build` → 設 git author → worktree 準備（已存在則 fetch + add；不存在則 `--detach` 加 `checkout --orphan gh-pages` 加清空）→ `node scripts/publishPages.mjs --worktree .deploy-pages --dist dist --target "$PUBLISH_TARGET"` → `git add -A` →若 `diff --cached --quiet` 則 exit 0 否則 commit `deploy: publish ${PUBLISH_TARGET}` 與 push。完成定義：`npm run test:unit -- githubActionsWorkflows` 通過 CD workflow 相關斷言。
- [x] 5.3 依「Publish with a gh-pages worktree and guarded sync script」實作 `Protected GitHub Pages publishing`：建立 `scripts/publishPages.mjs` ESM 純 Node 22 腳本，無第三方依賴；定義 `ROOT_ENTRIES_ALWAYS_PRESERVED = new Set(['.git', '.nojekyll', 'CNAME', 'staging'])`；export `syncPublishedSite({ worktreeRoot, distPath, target })`、`formatPublishSummary`、`formatNoPublishChangesMessage`；行為依 spec scenarios（production 清 root 保留清單外、staging 清 `staging/` 子目錄、staging 刪含 `/src/` 的 stale index.html、複製跳過 `.gz`/`.br`、結束寫 `.nojekyll`、空 dist throw、未知 target throw）；CLI 入口解析 `--worktree`/`--dist`/`--target` 並呼叫 `syncPublishedSite`，列印 `formatPublishSummary`。完成定義：`npm run test:unit -- publishPages` 全部通過。

## 6. 文件與架構記錄

- [x] 6.1 依「Document repository operations and architecture impact」實作 `CI/CD documentation` 與 `First-deploy operational prerequisites`：建立 `README.md`（目前 repo 無此檔），含「本機驗證」（6 個 npm script 對照表）、「CI 流程」（觸發條件、步驟摘要、diagnostics artifact）、「CD 流程」（`dev → staging` 與 `main → production` 表）、「部署 URL」（production 與 staging URL pattern）、「Pages 設定」（Settings → Pages 選 `gh-pages` root）、「branch protection 建議」、以及一個 `## 首次部署 First-deploy Checklist` 章節列出 6 個步驟（建 dev 分支、建 main 分支、設 Pages source、驗 production URL 200、驗 staging URL 200、設 branch protection）；建立 `PROJECT_ARCHITECTURE.md`，含新增檔案清單（含一句話說明）與 base path → target → URL 對照表。完成定義：`npm run test:unit -- readmeCiCdDocs` 通過，且人工逐項對照確認兩份文件無互相矛盾。

## 7. 最終驗證

- [x] 7.1 在本機端到端驗證：執行 `npm run test:ci`（lint && typecheck && test:unit && build && test:e2e）全部通過，再執行 `spectra analyze setting-ci-cd --json` 與 `spectra validate setting-ci-cd` 都通過；整理完成摘要含 created files 清單、CI 觸發行為、CD 分支對 target 映射、本機驗證結果、以及 `First-deploy operational prerequisites` 中需要維護者在 GitHub UI 完成的步驟（提醒尚未執行）。完成定義：完成摘要以 markdown 列出上述五個段落。
