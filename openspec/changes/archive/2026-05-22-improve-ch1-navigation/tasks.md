## 1. 撰寫測試契約（TDD：先紅後綠）

- [x] [P] 1.1 Create `src/__tests__/BackToTopFab.test.ts` covering the "BackToTopFab 用 IntersectionObserver 監看 quick nav 元素" design decision and the FAB SHALL appear when the quick-nav toolbar is out of view requirement: mock `IntersectionObserver` to simulate `isIntersecting=true` and `false` transitions, assert (a) when `anchorEl` is null, no `[data-testid="back-to-top-fab"]` renders; (b) when observer reports `isIntersecting=true`, FAB hidden; (c) when observer reports `isIntersecting=false`, FAB renders; (d) clicking FAB calls `window.scrollTo` with `{top:0, behavior:"smooth"}`; (e) `offsetBottom=60` prop produces inline style `bottom: calc(1.5rem + 60px)` per the "Fab 並存：BackToTopFab 在上、BackToWordFab 在下，靠 offsetBottom prop 控制" decision. Verification: `npx vitest run src/__tests__/BackToTopFab.test.ts` shows red before implementation.

- [x] [P] 1.2 Create `src/__tests__/SectionQuickNav.test.ts` covering the "Smooth scroll 用 scrollIntoView，section 加固定 id" design decision and the Ch1 page SHALL provide an in-page section quick-navigation toolbar requirement, plus the "快捷導覽列不 sticky" non-stick requirement (toolbar element has no `sticky` Tailwind class): mount with `sections = [{id:'a',label:'A'},{id:'b',label:'B'}]`, assert (a) two buttons render with `data-testid="quick-nav-a"`/`"quick-nav-b"` and matching labels; (b) clicking a button calls `document.getElementById(section.id).scrollIntoView` with `{behavior:"smooth", block:"start"}`; (c) when `document.getElementById` returns null, click is silent no-op (no throw); (d) root element exposes `rootEl` via `defineExpose` so parents can use it as IntersectionObserver target; (e) root container has neither `sticky` nor `fixed` Tailwind class. Verification: `npx vitest run src/__tests__/SectionQuickNav.test.ts` shows red before implementation.

- [x] [P] 1.3 Append assertions to `src/__tests__/NavBar.test.ts` covering the "NavBar Ch1 按鈕順序：[首頁] [Ch1] [文法]" decision and NavBar SHALL provide a direct entry to the Ch1 content route requirement: (a) `[data-testid="nav-ch1"]` exists with visible text "Ch1"; (b) the anchor's `href` resolves to `/ch1`; (c) the three nav buttons appear in DOM order "首頁", "Ch1", "文法" — confirming the "[首頁] [Ch1] [文法]" order; (d) clicking the Ch1 button triggers router navigation to `/ch1` (mock router); (e) when `useRoute` indicates current path is `/ch1`, the Ch1 button has the active highlight classes (`bg-terracotta text-white`). Verification: `npx vitest run src/__tests__/NavBar.test.ts` shows the new assertions failing before implementation.

- [x] [P] 1.4 Append assertions to `src/__tests__/Ch1View.test.ts` covering Ch1 page sections SHALL have stable anchor ids plus the "抽出共用元件 SectionQuickNav.vue 與 BackToTopFab.vue 至 src/shared/components/" decision (both new components must be wired into Ch1View): (a) DOM contains exactly one element with each of the four ids `ch1-section-bilingual`, `ch1-section-vocabulary`, `ch1-section-phrases`, `ch1-section-breakdown`; (b) each id maps to the section containing the expected content (e.g. `ch1-section-bilingual` contains scenes from `scenes` array); (c) `SectionQuickNav` component renders with `sections` prop length 4 and labels `['全文', '單字', '片語', '句型']`; (d) `BackToTopFab` component renders inside Ch1View. Verification: `npx vitest run src/__tests__/Ch1View.test.ts` shows the new assertions failing before implementation.

## 2. 實作新增共用元件

- [x] [P] 2.1 Implement `src/shared/components/BackToTopFab.vue` realizing the "BackToTopFab 用 IntersectionObserver 監看 quick nav 元素" decision: accept props `anchorEl: HTMLElement | null` and `offsetBottom?: number` (default 0); use `watch` on `anchorEl` to (dis)connect `IntersectionObserver`; render `<button>` with `data-testid="back-to-top-fab"` `fixed right-6 z-40` and inline style `bottom: calc(1.5rem + ${offsetBottom}px)`, visible when `!isIntersecting`; click handler calls `window.scrollTo({top:0, behavior:"smooth"})`; reuse fade `<Transition>` pattern from existing `BackToWordFab.vue`; `disconnect()` observer on `onUnmounted`. The component completes one half of the "抽出共用元件 SectionQuickNav.vue 與 BackToTopFab.vue 至 src/shared/components/" decision. Verification: test 1.1 turns green.

- [x] [P] 2.2 Implement `src/shared/components/SectionQuickNav.vue` realizing the "Smooth scroll 用 scrollIntoView，section 加固定 id" decision and the "快捷導覽列不 sticky" decision: accept prop `sections: Array<{id: string; label: string}>`; render container `<div ref="rootEl">` with `data-testid="ch1-quick-nav"` `flex flex-nowrap gap-2 overflow-x-auto py-3` — explicitly NO `sticky`/`fixed` classes per the non-stick decision; for each section render `<button data-testid="quick-nav-{id}">{label}</button>` with shared button classes (`px-3 py-1.5 rounded text-sm font-medium bg-paper-2 border border-line text-ink hover:bg-line transition-colors`); click handler calls `document.getElementById(id)?.scrollIntoView({behavior:"smooth", block:"start"})` with optional chaining for null safety; `defineExpose({ rootEl })`. The component completes the other half of the "抽出共用元件 SectionQuickNav.vue 與 BackToTopFab.vue 至 src/shared/components/" decision. Verification: test 1.2 turns green.

## 3. 整合進既有頁面

- [x] [P] 3.1 Update `src/modules/ch1/views/Ch1View.vue` realizing the "Smooth scroll 用 scrollIntoView，section 加固定 id" decision and the "Fab 並存：BackToTopFab 在上、BackToWordFab 在下，靠 offsetBottom prop 控制" decision: (a) add `id="ch1-section-bilingual"` on the bilingual section, `id="ch1-section-vocabulary"` on the vocabulary section, `id="ch1-section-phrases"` on the phrases section, `id="ch1-section-breakdown"` on the breakdown section — providing stable anchor ids for the Ch1 page sections SHALL have stable anchor ids requirement; (b) import and render `<SectionQuickNav ref="quickNavRef" :sections="ch1Sections" />` between header and the first section; (c) declare `const ch1Sections = [{id:'ch1-section-bilingual', label:'全文'}, {id:'ch1-section-vocabulary', label:'單字'}, {id:'ch1-section-phrases', label:'片語'}, {id:'ch1-section-breakdown', label:'句型'}]`; (d) import and render `<BackToTopFab :anchor-el="quickNavRootEl" :offset-bottom="sourceScrollY !== null ? 60 : 0" />` — the offset binding realizes the back-to-top FAB SHALL coexist with the existing back-to-word FAB without overlap requirement by stacking BackToTopFab 60px above BackToWordFab when both are visible; (e) wire `quickNavRootEl` via `quickNavRef.value?.rootEl` in `onMounted` after `nextTick`. Verification: test 1.4 turns green; manual mobile viewport scroll inspection confirms behavior in task 4.2.

- [x] [P] 3.2 Update `src/shared/components/NavBar.vue` realizing the "NavBar Ch1 按鈕順序：[首頁] [Ch1] [文法]" decision and the NavBar SHALL provide a direct entry to the Ch1 content route requirement: insert a third `<RouterLink to="/ch1" custom v-slot="...">` between the existing 首頁 and 文法 RouterLinks — yielding the exact button order [首頁] [Ch1] [文法]; the anchor element MUST have `data-testid="nav-ch1"`, visible text "Ch1", and use the exact same Tailwind class array `'px-4 py-1.5 rounded text-sm font-medium transition-colors'` plus the active-state binding `isActive ? 'bg-terracotta text-white' : 'bg-paper text-ink hover:bg-line'`. Verification: test 1.3 turns green.

## 4. 驗證與回歸

- [x] 4.1 Run `npm run test:unit` and confirm the entire vitest suite passes — the 2 new test files (`BackToTopFab.test.ts`, `SectionQuickNav.test.ts`) and the 2 updated test files (`NavBar.test.ts`, `Ch1View.test.ts`) all turn green, AND every other existing test file (Ch1View original assertions, NavBar original assertions, router, scaffold, smoke, Mp3Player, useCompletion, useReadingBookmark, useUnderlinkBacklink, GrammarView, plus the 5 CI/CD-pipeline contract tests) continues to pass with zero regressions. Verification: vitest output shows all test files green; total test count increases by at least 8 (2 new files + appended assertions).

- [x] 4.2 Run `npm run dev`, open `/ch1` in Chrome DevTools using the iPhone 14 preset (390×844 viewport), and manually verify the four happy paths covering back-to-top FAB SHALL appear when the quick-nav toolbar is out of view and back-to-top FAB SHALL coexist with the existing back-to-word FAB without overlap: (a) quick-nav toolbar visible below header with 4 buttons "全文/單字/片語/句型"; clicking each button smooth-scrolls to the matching section; (b) scroll down past the toolbar — back-to-top FAB appears bottom-right; click FAB — page smooth-scrolls to top and FAB disappears; (c) click an underlined word in the bilingual section — BackToWordFab appears at `bottom: 1.5rem`; while it is visible, scroll down past the toolbar — BackToTopFab appears stacked 60px above BackToWordFab with no overlap; (d) navigate `/ch1` → `/grammar` → click NavBar 內容 dropdown → select 我的紐約之旅 — lands on `/ch1` directly without going through `/`.  Verification: all four scenarios visually confirmed; capture observations or screenshots.

## 5. NavBar 改用「內容 ▾」dropdown（apply 階段反饋的擴充性修正）

- [x] 5.1 Create `src/shared/config/chapters.ts` realizing the "共用 chapters config 作為單一資料源" decision and the Chapter list SHALL be defined in a single shared config requirement: export an interface `ChapterEntry` with fields `id: string`, `path: string`, `shortLabel: string`, `titleZh: string`, `titleEn: string`; export a `chapters: ChapterEntry[]` array containing one entry today (`id: 'ch1'`, `path: '/ch1'`, `shortLabel: 'Ch1'`, `titleZh: '我的紐約之旅'`, `titleEn: 'My Trip to New York City · Slow English Podcast B1'`). Verification: TypeScript compile (`npm run typecheck`) passes; subsequent NavBar and HomeView refactors import from this module without errors.

- [x] 5.2 Rewrite `src/__tests__/NavBar.test.ts` to test the dropdown behavior realizing the "NavBar 採用「內容 ▾」dropdown 而非直連單章按鈕" decision and the NavBar SHALL provide an extensible chapter-content dropdown requirement: replace all `nav-ch1` direct-link assertions with dropdown assertions covering (a) initial state — `nav-content-trigger` exists with text containing "內容" and `aria-haspopup="menu"`, `nav-content-menu` not rendered; (b) click trigger — `nav-content-menu` becomes visible and contains exactly one `nav-content-ch1` button with the chapter `titleZh` "我的紐約之旅"; (c) click `nav-content-ch1` — router.currentRoute.value.path becomes `/ch1` and menu element is no longer rendered; (d) on `/ch1` route — trigger has `bg-terracotta` `text-white` active classes; (e) on `/` or `/grammar` route — trigger does NOT have active classes; (f) menu open then click outside the dropdown container — menu closes; (g) menu open then dispatch `Escape` keydown — menu closes. Verification: tests are red before NavBar refactor in task 5.3.

- [x] 5.3 Refactor `src/shared/components/NavBar.vue` realizing the "NavBar 採用「內容 ▾」dropdown 而非直連單章按鈕" decision: remove the hardcoded `nav-ch1` RouterLink added in task 3.2; in its place between `nav-home` and `nav-grammar` add a `<div class="relative">` containing (a) a `<button data-testid="nav-content-trigger" aria-haspopup="menu" :aria-expanded="isOpen">內容 ▾</button>` reusing the existing button class array including the active-state binding `isAnyChapterActive ? 'bg-terracotta text-white' : 'bg-paper text-ink hover:bg-line'`, and (b) a `<Transition>`-wrapped panel `<div v-if="isOpen" data-testid="nav-content-menu" role="menu">` with absolute positioning below the trigger, listing one `<button data-testid="nav-content-{chapter.id}">{chapter.titleZh}</button>` per entry from the imported `chapters` config; clicking a chapter calls `router.push(chapter.path)` then sets `isOpen.value = false`; mount document `click` and `keydown` listeners to close menu on outside-click and Escape; remove listeners on unmount. The component MUST import `chapters` from `../config/chapters` and MUST NOT hardcode any chapter id, path, or title. Verification: task 5.2 tests turn green; `npm run typecheck` passes.

- [x] 5.4 Refactor `src/modules/home/views/HomeView.vue` realizing the "共用 chapters config 作為單一資料源" decision: import `chapters` from `../../../shared/config/chapters`; replace the hardcoded `<ArticleListItem title="我的紐約之旅" ... @navigate="router.push('/ch1')" ...>` with `<ArticleListItem v-for="chapter in chapters" :key="chapter.id" :title="chapter.titleZh" :subtitle="chapter.titleEn" :completed="isCompleted(chapter.id)" :data-testid="`article-${chapter.id}`" @navigate="router.push(chapter.path)" @toggle-completion="toggleCompletion(chapter.id)" />`. HomeView MUST NOT hardcode any chapter id, path, or title string after this refactor. Verification: existing `npm run test:unit` Ch1View/home-related assertions still pass; visiting `/` still shows the ch1 article item with same title and behavior.

- [x] 5.5 Re-run `npm run test:unit` and confirm full suite passes after the dropdown refactor: NavBar tests reflect new dropdown behavior (no `nav-ch1` direct link assertion remains), HomeView still renders ch1 article from `chapters` config, all 138+ previously-green tests stay green, total test count remains stable or increases. Verification: vitest shows green across all 17+ test files.

## 6. 滾動補正（apply 階段反饋的視覺修正）

- [x] 6.1 Add global "全域 `scroll-padding-top` 處理 sticky NavBar 遮擋" CSS rule in `src/style.css`: `html { scroll-padding-top: calc(4rem + env(safe-area-inset-top, 0px)); }`. This covers every `scrollIntoView` call in the app — section quick-nav targets, reading-bookmark scene targets, and any future anchored scrolls — without per-element class. Verification: `npm run test:unit` stays green; manual viewport check in task 4.2 confirms section headings remain visible below NavBar after each quick-nav click.
