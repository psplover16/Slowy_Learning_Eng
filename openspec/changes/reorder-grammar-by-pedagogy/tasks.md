## 1. 撰寫測試（TDD：先紅後綠）

- [ ] 1.1 Add Vitest test in `src/__tests__/GrammarView.test.ts` that asserts grammar cards SHALL be organized into pedagogical thematic groups: query all `[data-testid="grammar-card"]`, extract each card's `<h3>` title text, and assert the resulting array matches the exact 18-topic sequence — Parts of Speech → Noun Phrase → Relative Clause → Participial Phrase → used to → Present Perfect comparison → Perfect Infinitive → Preposition + V-ing → while + V-ing → for duration → if dual usage → so that → Causative Verbs → get usage → like usage → prefer → ever → just as...as. Verification: `npm run test:unit -- GrammarView` shows this assertion failing before any view edits.

- [ ] 1.2 Add Vitest test in `src/__tests__/GrammarView.test.ts` that each thematic group SHALL display a visible section header: assert exactly 7 elements matching `[data-testid="grammar-section-header"]` are rendered, and that their text contents equal `['1. 詞類基礎', '2. 名詞片語家族', '3. 動詞時態', '4. V-ing 後接慣例', '5. 從屬子句與時間', '6. 多功能動詞', '7. 語氣與比較']` in DOM order. Verification: `npm run test:unit -- GrammarView` shows this assertion failing before any view edits.

- [ ] 1.3 Add Vitest test in `src/__tests__/GrammarView.test.ts` that grammar card badges SHALL be numbered consecutively in display order: collect each card's badge text (the `<span>` containing `G##` inside the GrammarCard `<h3>`) in DOM order and assert the array equals `['G01', 'G02', 'G03', 'G04', 'G05', 'G06', 'G07', 'G08', 'G09', 'G10', 'G11', 'G12', 'G13', 'G14', 'G15', 'G16', 'G17', 'G18']`. Verification: `npm run test:unit -- GrammarView` shows this assertion failing before any view edits.

## 2. 重排 GrammarView.vue 卡片與編號

- [ ] 2.1 In `src/modules/grammar/views/GrammarView.vue` template, reorder the 18 `<GrammarCard>` blocks inside `<main>` to the new top-to-bottom sequence defined in proposal.md (Parts of Speech first, just as...as last). Do not edit any card's inner content (text, examples, tables, styling tokens). Verification: test 1.1 turns green; `npm run test:unit -- GrammarView` passes the ordering assertion.

- [ ] 2.2 In `src/modules/grammar/views/GrammarView.vue`, renumber every `<GrammarCard :badge="...">` prop so badges run G01 through G18 consecutively in the new display order — the first rendered card receives badge `"G01"`, the second `"G02"`, continuing through the 18th card with badge `"G18"`. Verification: test 1.3 turns green; manual visual scroll confirms sequential numbering with no gaps or duplicates.

- [ ] 2.3 In `src/modules/grammar/views/GrammarView.vue`, insert 7 section header elements `<h2 data-testid="grammar-section-header">` immediately above the first card of each thematic group, with the exact text contents `1. 詞類基礎`, `2. 名詞片語家族`, `3. 動詞時態`, `4. V-ing 後接慣例`, `5. 從屬子句與時間`, `6. 多功能動詞`, `7. 語氣與比較`. Style with Tailwind utilities so each header is visually distinct from grammar cards (e.g., larger Fraunces font, extra top margin separating from the previous group, teal-eng-deep color). Verification: test 1.2 turns green; on the mobile viewport, each group boundary is visually perceivable while scrolling.

## 3. 驗證與回歸

- [ ] 3.1 Run full unit test suite via `npm run test:unit` and confirm all tests pass — both the 3 new GrammarView assertions (pedagogical group ordering, section headers, consecutive badges) and the existing `src/__tests__/*.test.ts` suites (Ch1View, NavBar, router, Mp3Player, HomeView, useCompletion, useReadingBookmark, useUnderlinkBacklink, scaffold, smoke) continue to pass with zero regressions. Verification: vitest output shows all test files green with no failed assertions.

- [ ] 3.2 Start dev server with `npm run dev`, open `/grammar` in Chrome DevTools using the iPhone 14 preset (390×844), and manually verify by scrolling top to bottom: (a) the 18 cards appear in the new topic sequence, (b) 7 section headers are visible above their groups and visually distinct from cards, (c) badges read G01 through G18 sequentially with no gaps, (d) the page still works offline — toggle DevTools "Offline" and reload, page renders from the Service Worker cache with no console errors. Verification: all four manual checks pass; record observations or a screenshot.
