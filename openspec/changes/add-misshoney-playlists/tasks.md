<!--
暫停規則：
- 本檔列的是完整目標，全部 checkbox 完成才代表 MissHoney 全量功能完成。
- 若本次 apply 要暫停某段，請先把要暫停的 checkbox 移到檔尾「Paused - Not Part of This Apply」並移除 `- [ ]` checkbox 語法；不要把未完成任務標成 `[x]`。
- 若只想口頭指定，請用 task group 編號說明，例如「本次先跳過第 8 組 A2-B2 content authoring」；執行者必須保持被跳過 task 未勾選。
-->

## 1. Foundation - Types, Storage Key, Architecture Contract

- [ ] [P] 1.1 Define playlist TypeScript interfaces in `src/modules/playlists/types.ts`: `PlaylistData`, `PlaylistVideoEntry`, `SkippedVideoEntry`, `PlaylistVideoData`, `PlaylistScene`, `PlaylistVocabGroup`, `PlaylistPhrase`, and any import/content report interfaces needed by tooling. Satisfies: JSON content structure; Video content status display. Verify: `npm run build` compiles `types.ts` without errors.
- [ ] [P] 1.2 Add `MISS_HONEY_COMPLETION: 'slowy:miss-honey-completion'` to the exported `STORAGE_KEYS` object in `src/shared/config/storageKeys.ts`. Satisfies: Storage key isolation. Verify: property present in `STORAGE_KEYS`; `npm run build` passes.
- [ ] 1.3 Update `PROJECT_ARCHITECTURE.md` so the new `src/modules/playlists/`, `scripts/misshoney/`, and generated content flow are documented as part of the project architecture. Verify: the document names the MissHoney module, import scripts, content promotion flow, and route ownership.

## 2. YouTube Import Tool - Automated Playlist and Caption Fetching

- [ ] 2.1 Create `scripts/misshoney/sources.json` containing exactly four source playlists in fixed order A1, A2, B1, B2. Each entry includes `level`, `title`, and `playlistUrl`. Satisfies: Import tool fetches playlists and captions automatically. Verify: JSON parses and contains exactly the four expected levels.
- [ ] 2.2 Write failing tests for importer pure functions covering reverse playlist ordering, skipped-video exclusion from `displayOrder`, `ch[displayOrder]-[kebab-title]` slug derivation, skipped reason mapping (`member-only`, `private`, `no-english-captions`, `unavailable`, `geo-restricted`), transcript cue normalization, and output planning that writes only `_private/misshoney/**`. Satisfies: Import helper functions are testable without network access; Video slug format; Import output is generated under `_private/misshoney`; Import tool handles unavailable captions and videos safely. Verify: `npm run test` reports these importer tests failing before implementation.
- [ ] 2.3 Create `scripts/misshoney/import-core.mjs` implementing the tested pure functions from task 2.2. The helpers accept raw playlist/caption metadata and return normalized inventory, transcript, and skipped-report records without network access. Satisfies: Import helper functions are testable without network access; Video slug format. Verify: importer tests from task 2.2 pass.
- [ ] 2.4 Create `scripts/misshoney/import-playlists.mjs` CLI. It reads `sources.json`, invokes `yt-dlp` from `YT_DLP_PATH` or `PATH`, fetches public playlist metadata and English captions/auto-captions, writes only `_private/misshoney/inventory/<level>.json`, `_private/misshoney/transcripts/<level>/<slug>.json`, `_private/misshoney/skipped/<level>.json`, and `_private/misshoney/import-summary.json`, reports per-level imported/skipped counts, and supports `--check-existing` to verify existing raw outputs. Satisfies: Import tool fetches playlists and captions automatically; Import output is generated under `_private/misshoney`; Import tool handles unavailable captions and videos safely. Verify: `node scripts/misshoney/import-playlists.mjs --help` exits 0; missing `yt-dlp` exits non-zero with a message naming `YT_DLP_PATH`.
- [ ] 2.5 Add npm script `misshoney:import` that runs `node scripts/misshoney/import-playlists.mjs`. Satisfies: Import tool fetches playlists and captions automatically. Verify: `npm run misshoney:import -- --help` exits 0.
- [ ] 2.6 Add `_private/misshoney/README.md` documenting raw output folders, re-run behavior, skipped reasons, and the rule that manual subtitle fetching is out of scope. Verify: README names `npm run misshoney:import`, `_private/misshoney/inventory`, `_private/misshoney/transcripts`, and `_private/misshoney/skipped`.

## 3. Full Subtitle Extraction - A1 to B2 Raw Data

- [ ] 3.1 Run `npm run misshoney:import` for all configured levels and keep the generated raw outputs under `_private/misshoney/`. Satisfies: Import tool fetches playlists and captions automatically; Import output is generated under `_private/misshoney`. Verify: inventory, transcripts, and skipped files exist for A1, A2, B1, and B2; command output does not ask for manual subtitle copying.
- [ ] 3.2 Review `_private/misshoney/skipped/<level>.json` for A1, A2, B1, and B2 so every skipped entry has `youtubeUrl`, `originalIndex`, and one allowed reason. Satisfies: Import tool handles unavailable captions and videos safely; Skipped videos are not routable. Verify: skipped reports parse as JSON and contain no unknown reason values.
- [ ] 3.3 Review import coverage counts so every non-skipped inventory item has exactly one transcript file at `_private/misshoney/transcripts/<level>/<slug>.json`. Satisfies: Complete content coverage is verifiable across all levels. Verify: `npm run misshoney:import -- --check-existing` exits 0 and `_private/misshoney/import-summary.json` reports zero missing transcript files.

## 4. Content Generation, Validation, and Promotion Tooling

- [ ] 4.1 Write failing tests for content helper functions covering transcript-to-scaffold creation, source cue preservation, sentence pair completeness, vocab/phrase array validation, slug/videoId/level consistency, safe promotion planning, and all-level coverage reporting. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript; Content authoring fills PlaylistVideoData drafts from scaffolds; Content validation blocks incomplete learning content; Content promotion wires complete videos into app routes; Complete content coverage is verifiable across all levels. Verify: `npm run test` reports these content tooling tests failing before implementation.
- [ ] 4.2 Create `scripts/misshoney/content-core.mjs` implementing pure helpers for scaffold creation, schema validation, completeness validation, promotion planning, and coverage counting. The helpers do not translate text or choose teaching vocabulary; they only prepare source material and validate authored JSON. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript; Content authoring fills PlaylistVideoData drafts from scaffolds; Content validation blocks incomplete learning content. Verify: tests from task 4.1 pass for pure helpers.
- [ ] 4.3 Create `scripts/misshoney/scaffold-content.mjs` CLI. It reads `_private/misshoney/transcripts/<level>/*.json` and writes authoring scaffolds to `_private/misshoney/content-scaffolds/<level>/<slug>.json` without changing app content or producing final Traditional Chinese translations, `vocabGroups`, or `phrases`. It supports `--level <level>` and `--all`. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript. Verify: `node scripts/misshoney/scaffold-content.mjs --help` exits 0 and a fixture run creates one scaffold per transcript.
- [ ] 4.4 Create `scripts/misshoney/validate-content.mjs` CLI. It validates generated and promoted content for required fields, non-empty Traditional Chinese translations, non-empty scenes/vocabGroups/phrases, slug/videoId/level consistency, and transcript/generated/promoted coverage. It supports `--level <level>`, `--all`, `--generated-only`, and `--raw-only`. Satisfies: Content validation blocks incomplete learning content; Complete content coverage is verifiable across all levels. Verify: invalid fixtures exit non-zero with file and field names; valid fixtures exit 0.
- [ ] 4.5 Create `scripts/misshoney/promote-content.mjs` CLI. It copies only validated generated content into `src/modules/playlists/data/videos/<level>/`, regenerates or updates the matching `src/modules/playlists/data/<level>.ts`, keeps skipped videos out of `videos`, supports `--level <level>` and `--all`, and refuses to promote when validation fails. Satisfies: Content promotion wires complete videos into app routes; Skipped videos are not routable. Verify: fixture promotion creates ready entries with non-null `contentLoader` and does not create JSON for skipped videos.
- [ ] 4.6 Add npm scripts `misshoney:scaffold-content`, `misshoney:validate-content`, and `misshoney:promote-content`. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript; Content validation blocks incomplete learning content; Content promotion wires complete videos into app routes. Verify: each script supports `--help` and exits 0.

## 5. Playlist Config, Completion, and Base Metadata

- [ ] [P] 5.1 Create `src/shared/config/playlists.ts` exporting a `playlists` array with four entries ordered A1, A2, B1, B2. Each entry contains `id`, `path`, `shortLabel`, and `titleZh`. Satisfies: Difficulty display order is fixed from easy to hard. Verify: `npm run build` imports the file without errors.
- [ ] 5.2 Write failing unit tests for `useMissHoneyCompletion` covering video completion state toggle, completion state persistence across reloads, storage key isolation from `slowy:completion`, corrupt JSON recovery, and localStorage write failure. Satisfies: Video completion state toggle; Completion state persists across reloads; Storage key isolation; Graceful failure on localStorage error. Verify: `npm run test` reports failures for these cases.
- [ ] 5.3 Create `src/modules/home/composables/useMissHoneyCompletion.ts`. It follows the same JSON-map pattern as `useCompletion.ts`, reads/writes `STORAGE_KEYS.MISS_HONEY_COMPLETION`, and exports `completionMap`, `isCompleted(videoId: string)`, and `toggleCompletion(videoId: string)`. Satisfies: Video completion state toggle; Completion state persists across reloads; Storage key isolation; Graceful failure on localStorage error. Verify: tests from task 5.2 pass.
- [ ] 5.4 Create initial `src/modules/playlists/data/a1.ts`, `a2.ts`, `b1.ts`, and `b2.ts` metadata modules that can be regenerated by promotion. Before full promotion, entries with transcripts but no promoted JSON use `pendingTranscript`; skipped report entries stay in `skippedVideos`. Satisfies: Four CEFR-level playlist entry routes are accessible; Video content status display; Skipped videos are not routable; Video slug format. Verify: TypeScript type-check passes and skipped videos are absent from each `videos` array.

## 6. Playlist Routes and Views

- [ ] 6.1 Write failing component tests for `PlaylistView` covering four CEFR-level playlist entry routes, data loading by level, skipped videos absent from cards, card links to `/:level/:slug`, cards sorted by `displayOrder` ascending, and completion circle rendering per video. Satisfies: Four CEFR-level playlist entry routes are accessible; Video sub-page routes are accessible for learnable videos; Difficulty display order is fixed from easy to hard; Skipped videos are not routable. Verify: `npm run test` reports failures for `PlaylistView`.
- [ ] 6.2 Create `src/modules/playlists/PlaylistView.vue`. It accepts level prop, lazy-loads the matching data module, renders video cards in ascending `displayOrder`, hides `skippedVideos`, shows title/subtitle and a completion circle wired to `useMissHoneyCompletion`, and navigates to `/:level/:slug`. Satisfies: Four CEFR-level playlist entry routes are accessible; Video sub-page routes are accessible for learnable videos; Difficulty display order is fixed from easy to hard; Skipped videos are not routable. Verify: tests from task 6.1 pass.
- [ ] 6.3 Write failing component tests for `PlaylistVideoView` covering ready status loading JSON, `pendingTranscript` showing `內容整理中`, unknown slug showing `找不到此影片`, scenes/vocabGroups/phrases rendering, and `highlight: true` visual emphasis. Satisfies: Video content status display; JSON content structure. Verify: `npm run test` reports failures for `PlaylistVideoView`.
- [ ] 6.4 Create `src/modules/playlists/PlaylistVideoView.vue`. It accepts `level` and `videoSlug`, finds the video in metadata, calls `contentLoader` only for `ready`, renders pending and not-found states, and displays scenes, vocabGroups, and phrases from pure JSON. Satisfies: Video content status display; JSON content structure. Verify: tests from task 6.3 pass.
- [ ] 6.5 Add eight lazy-loaded routes to `src/app/router/index.ts`: `/a1`, `/a2`, `/b1`, `/b2`, plus `/a1/:videoSlug`, `/a2/:videoSlug`, `/b1/:videoSlug`, and `/b2/:videoSlug`. Satisfies: Four CEFR-level playlist entry routes are accessible; Video sub-page routes are accessible for learnable videos. Verify: `npm run build` succeeds and router tests resolve `/a1` plus one video route per level.

## 7. NavBar, HomeView, and ArticleListItem Integration

- [ ] 7.1 Write failing tests for `NavBar` covering MissHoney button order between `內容` and `文法`, active style on MissHoney routes, dropdown items A1/A2/B1/B2 in order, navigation on item click, close on outside click/Escape, and overflow class `max-w-[calc(100vw-11rem)]`. Satisfies: NavBar contains MissHoney entry; MissHoney dropdown button in NavBar; MissHoney dropdown menu contains A1–B2 items; Dropdown overflow prevention. Verify: `npm run test` reports failures.
- [ ] 7.2 Add MissHoney dropdown to `src/shared/components/NavBar.vue`. It reuses the existing dropdown interaction pattern, anchors at `left-0`, uses `w-max max-w-[calc(100vw-11rem)]`, and keeps existing 首頁/內容/文法 behavior intact. Satisfies: NavBar contains MissHoney entry; MissHoney dropdown button in NavBar; MissHoney dropdown menu contains A1–B2 items; Dropdown overflow prevention. Verify: tests from task 7.1 pass.
- [ ] 7.3 Write failing tests for `ArticleListItem` covering default/no prop shows completion circle, `showCompletion=true` shows circle, and `showCompletion=false` hides circle while preserving card title/subtitle/navigation click. Satisfies: ArticleListItem supports optional completion circle. Verify: `npm run test` reports the new test as failing.
- [ ] 7.4 Add optional boolean prop `showCompletion` defaulting to true in `src/modules/home/components/ArticleListItem.vue`, and render the completion circle only when true. Satisfies: ArticleListItem supports optional completion circle. Verify: tests from task 7.3 pass and existing ch1-ch4 usage still shows circles.
- [ ] 7.5 Write failing tests for `HomeView` covering MissHoney section below existing ch1-ch4 article list, four cards A1/A2/B1/B2 in order, MissHoney cards without completion circles, and existing ch1-ch4 cards still showing completion circles. Satisfies: MissHoney section on homepage; MissHoney homepage cards do not show completion circles; Existing article list is unaffected. Verify: `npm run test` reports failures.
- [ ] 7.6 Add a MissHoney section to `src/modules/home/views/HomeView.vue` below the existing article list. It renders four `ArticleListItem` cards from `playlists` with `showCompletion=false`, navigates to `/a1` through `/b2`, and leaves the existing article list unchanged. Satisfies: MissHoney section on homepage; MissHoney homepage cards do not show completion circles; Existing article list is unaffected. Verify: tests from task 7.5 pass.

## 8. Full Content Scaffolding, Authoring, and Promotion by Level

- [ ] 8.1 Generate A1 authoring scaffolds for every raw transcript under `_private/misshoney/transcripts/a1/`. The command creates source-material scaffolds only; it does not pretend to translate or teach automatically. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript. Verify: `npm run misshoney:scaffold-content -- --level a1` completes and creates one scaffold per transcript.
- [ ] 8.2 Author A1 `PlaylistVideoData` drafts from A1 scaffolds/transcripts. The apply agent fills Traditional Chinese sentence translations, beginner-relevant `vocabGroups`, and useful `phrases` grounded in the source transcript for every A1 scaffold. Satisfies: Content authoring fills PlaylistVideoData drafts from scaffolds; JSON content structure. Verify: `npm run misshoney:validate-content -- --level a1 --generated-only` exits 0.
- [ ] 8.3 Promote A1 generated content into app data and routes. Every A1 transcript-backed video becomes `ready` with a non-null `contentLoader`; skipped A1 videos remain only in `skippedVideos`. Satisfies: Content promotion wires complete videos into app routes; Video sub-page routes are accessible for learnable videos; Skipped videos are not routable. Verify: `npm run misshoney:promote-content -- --level a1` exits 0 and `npm run misshoney:validate-content -- --level a1` exits 0.
- [ ] 8.4 Generate A2 authoring scaffolds for every raw transcript under `_private/misshoney/transcripts/a2/`. The command creates source-material scaffolds only; it does not pretend to translate or teach automatically. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript. Verify: `npm run misshoney:scaffold-content -- --level a2` completes and creates one scaffold per transcript.
- [ ] 8.5 Author A2 `PlaylistVideoData` drafts from A2 scaffolds/transcripts. The apply agent fills Traditional Chinese sentence translations, beginner-relevant `vocabGroups`, and useful `phrases` grounded in the source transcript for every A2 scaffold. Satisfies: Content authoring fills PlaylistVideoData drafts from scaffolds; JSON content structure. Verify: `npm run misshoney:validate-content -- --level a2 --generated-only` exits 0.
- [ ] 8.6 Promote A2 generated content into app data and routes. Every A2 transcript-backed video becomes `ready` with a non-null `contentLoader`; skipped A2 videos remain only in `skippedVideos`. Satisfies: Content promotion wires complete videos into app routes; Video sub-page routes are accessible for learnable videos; Skipped videos are not routable. Verify: `npm run misshoney:promote-content -- --level a2` exits 0 and `npm run misshoney:validate-content -- --level a2` exits 0.
- [ ] 8.7 Generate B1 authoring scaffolds for every raw transcript under `_private/misshoney/transcripts/b1/`. The command creates source-material scaffolds only; it does not pretend to translate or teach automatically. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript. Verify: `npm run misshoney:scaffold-content -- --level b1` completes and creates one scaffold per transcript.
- [ ] 8.8 Author B1 `PlaylistVideoData` drafts from B1 scaffolds/transcripts. The apply agent fills Traditional Chinese sentence translations, beginner-relevant `vocabGroups`, and useful `phrases` grounded in the source transcript for every B1 scaffold. Satisfies: Content authoring fills PlaylistVideoData drafts from scaffolds; JSON content structure. Verify: `npm run misshoney:validate-content -- --level b1 --generated-only` exits 0.
- [ ] 8.9 Promote B1 generated content into app data and routes. Every B1 transcript-backed video becomes `ready` with a non-null `contentLoader`; skipped B1 videos remain only in `skippedVideos`. Satisfies: Content promotion wires complete videos into app routes; Video sub-page routes are accessible for learnable videos; Skipped videos are not routable. Verify: `npm run misshoney:promote-content -- --level b1` exits 0 and `npm run misshoney:validate-content -- --level b1` exits 0.
- [ ] 8.10 Generate B2 authoring scaffolds for every raw transcript under `_private/misshoney/transcripts/b2/`. The command creates source-material scaffolds only; it does not pretend to translate or teach automatically. Satisfies: Content scaffolding creates authoring scaffolds for every imported transcript. Verify: `npm run misshoney:scaffold-content -- --level b2` completes and creates one scaffold per transcript.
- [ ] 8.11 Author B2 `PlaylistVideoData` drafts from B2 scaffolds/transcripts. The apply agent fills Traditional Chinese sentence translations, beginner-relevant `vocabGroups`, and useful `phrases` grounded in the source transcript for every B2 scaffold. Satisfies: Content authoring fills PlaylistVideoData drafts from scaffolds; JSON content structure. Verify: `npm run misshoney:validate-content -- --level b2 --generated-only` exits 0.
- [ ] 8.12 Promote B2 generated content into app data and routes. Every B2 transcript-backed video becomes `ready` with a non-null `contentLoader`; skipped B2 videos remain only in `skippedVideos`. Satisfies: Content promotion wires complete videos into app routes; Video sub-page routes are accessible for learnable videos; Skipped videos are not routable. Verify: `npm run misshoney:promote-content -- --level b2` exits 0 and `npm run misshoney:validate-content -- --level b2` exits 0.
- [ ] 8.13 Run all-level content coverage validation and fix any missing scaffold/generated/promoted content. Satisfies: Complete content coverage is verifiable across all levels; Content validation blocks incomplete learning content. Verify: `npm run misshoney:validate-content -- --all` exits 0 and reports matching transcript/scaffold/generated/promoted counts for A1, A2, B1, and B2.

## 9. Build, Regression, and Offline Verification

- [ ] 9.1 Run `npm run test` end-to-end and confirm all importer, content tooling, composable, router, NavBar, HomeView, ArticleListItem, PlaylistView, and PlaylistVideoView tests pass. Verify: test command exits 0.
- [ ] 9.2 Run `npm run build` end-to-end and confirm exit code 0 with no TypeScript errors, no Vite missing-import warnings, and chunk sizes within the existing project threshold. Verify: build output succeeds cleanly.
- [ ] 9.3 Start the app locally and smoke test `/a1`, `/a2`, `/b1`, `/b2`, plus at least two ready video sub-pages per level. Verify: each sampled sub-page displays scenes, vocabGroups with highlight styling, and phrases without console errors.
- [ ] 9.4 Verify offline behavior after the app has loaded once: open homepage, one playlist route, and one promoted video route while offline or with network disabled. Verify: app shell and promoted JSON content remain readable; no runtime YouTube/OpenAI/network dependency is required.
- [ ] 9.5 Verify completion persistence on a promoted video: click a completion circle, reload the route, and confirm the circle remains filled from `slowy:miss-honey-completion`. Verify: existing `slowy:completion` chapter progress is unchanged.
- [ ] 9.6 Verify existing `/ch1`, `/ch2`, `/ch3`, `/ch4`, `/grammar`, 首頁, 內容 dropdown, and 文法 navigation still behave as before. Verify: no existing chapter card loses its completion circle.

## 10. Final Manual Acceptance

- [ ] 10.1 Confirm `npm run misshoney:import`, `npm run misshoney:scaffold-content -- --all`, `npm run misshoney:validate-content -- --all`, and `npm run misshoney:promote-content -- --all` are documented and runnable. Verify: each command has `--help`, clear failure messages, and no instruction requiring manual subtitle copying or claiming deterministic translation.
- [ ] 10.2 Confirm every non-skipped transcript-backed video from A1, A2, B1, and B2 appears as a card in its level route and opens a working sub-page. Verify: coverage report shows zero missing promoted routes.
- [ ] 10.3 Confirm every skipped video from A1, A2, B1, and B2 is absent from playlist cards and returns `找不到此影片` if a guessed skipped-video URL is visited. Verify: skipped report count matches metadata `skippedVideos` count for each level.
- [ ] 10.4 Run `spectra validate add-misshoney-playlists` and confirm the change remains valid after implementation notes and task checkboxes are updated. Verify: command exits 0.

## Paused - Not Part of This Apply

Move task lines here only when the user explicitly chooses to pause them for a specific apply run. Do not mark paused tasks as complete.
